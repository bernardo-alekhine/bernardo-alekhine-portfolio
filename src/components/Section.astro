---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section
  id={id}
  class:list={[
    "w-full flex-1 flex flex-col justify-center items-center px-6 xl:px-30 gap-2 lg:gap-10 xl:gap-2",
    className,
  ]}
>
  <slot />
</section>
