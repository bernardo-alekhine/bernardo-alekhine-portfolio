---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className = "" } = Astro.props;
---

<section id={id} class={`w-full flex-1 flex flex-col justify-center items-center pb-10 ${className}`}>
  <slot />
</section>
