---
interface Props {
  gridCols: string;
}

const { gridCols = "" } = Astro.props;
---

<div
  class=`grid justify-items-center md:max-w-lg lg:max-w-2xl xl:max-w-xl 2xl:max-w-3xl mx-auto gap-x-3 gap-y-3 md:gap-x-8 md:gap-y-8 ${gridCols}`
>
  <slot />
</div>
