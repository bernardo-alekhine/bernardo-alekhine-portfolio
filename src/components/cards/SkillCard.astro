---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class:list={[
    "flex flex-col items-center px-6 gap-4 py-4 lg:gap-6 lg:px-16 lg:py-6 [&>svg]:select-none rounded-lg lg:rounded-xl drop-shadow-lg drop-shadow-card-shadow-color",
    className,
  ]}
>
  <slot />
</div>
