---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class:list={[
    "w-full flex flex-col items-center px-6 gap-4 py-8 md:gap-6 lg:px-16 lg:py-12 [&>svg]:select-none rounded-lg lg:rounded-xl \
    shadow-md shadow-card-shadow-color border border-card-shadow-color/50 transition-colors",
    className,
  ]}
>
  <slot />
</div>
