---
interface Props {
  title: string;
  svgBg?: string;
}

const { title, svgBg = "bg-card-work-tools" } = Astro.props;
---

<div
  class={`flex flex-col items-center px-6 gap-4 py-8 md:gap-6 lg:px-16 lg:py-12 [&>svg]:select-none rounded-lg \
  lg:rounded-xl shadow-md md:shadow-lg shadow-card-shadow-color transition-colors`}
>
  <strong class="text-card-title text-body-color">{title}</strong>
  <div class:list={`w-[70%] h-auto rounded-lg ${svgBg}`}>
    <slot />
  </div>
</div>
